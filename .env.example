# ============================================
# Leaf Application Environment Variables
# ============================================
# Copy this file to .env and update the values
# cp .env.example .env

# ============================================
# Server Configuration
# ============================================
# Port cho backend server (default: 5000)
PORT=5000

# Environment: development | production
NODE_ENV=development

# ============================================
# CORS Configuration
# ============================================
# CORS allowed origins (comma-separated)
# Example: http://localhost:3000,http://localhost:5000
CORS_ORIGIN=http://localhost:3000,http://localhost:5000

# ============================================
# Vite Configuration
# ============================================
# Port cho Vite dev server (default: 3000)
VITE_PORT=3000

# ============================================
# Blade Template Engine Configuration
# ============================================
# Đường dẫn thư mục chứa Blade templates (optional)
# Nếu không set, sẽ dùng: views/blade
# BLADE_VIEWS_DIR=views/blade

# ============================================
# SEO Configuration
# ============================================
# Base URL của website (dùng cho sitemap, OG tags, etc.)
BASE_URL=http://localhost:5000

# Tên website
SITE_NAME=Leaf App

# Open Graph image mặc định
DEFAULT_OG_IMAGE=/og-default.jpg

# Locale của website (format: vi_VN, en_US, etc.)
SITE_LOCALE=vi_VN

# Twitter handle (optional, để empty nếu không có)
TWITTER_HANDLE=

# ============================================
# Notes
# ============================================
# - UV_THREADPOOL_SIZE được tự động set dựa trên số CPU cores
# - Các giá trị mặc định đã được định nghĩa trong config/index.ts
# - Không commit file .env vào git (đã có trong .gitignore)
